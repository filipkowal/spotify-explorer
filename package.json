{
  "author": "Spotify",
  "name": "web-api-auth-examples",
  "description": "Basic examples of the Spotify authorization flows through OAuth 2",
  "version": "0.0.2",
  "engines": {
    "node": "14.5.x"
  },
  "dependencies": {
    "cookie-parser": "1.3.2",
    "express": "~4.16.0",
    "cors": "^2.8.4",
    "querystring": "~0.2.0",
    "request": "~2.83.0"
  },
  "scripts": {
    "start": "node authorization_code/app.js",
    "dev": "nodemon authorization_code/app.js",
    "build": "cd ./client && npm run build",
    "install-client": "cd ./client && npm install",
    "heroku-postbuild": "npm run build && npm run install-client"
  },
  "devDependencies": {
    "nodemon": "^2.0.7"
  }
}
